import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  loadMoreButton: {
    position: 'relative',
    bottom: 0,
    height: 60,
    textAlign: 'center',
    left: "45%",
    right: 0,
  }
});

export default useStyles;
